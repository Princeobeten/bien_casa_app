KYC IMPLEMENTATION PROGRESS

✅ COMPLETED:
1. Account Status Service (lib/app/services/account_status_service.dart)
   - GET /auth/account-status
   - POST /auth/send-otp/email
   - POST /auth/verify-email-otp
   - POST /misc/verify-nin
   - POST /misc/confirm-nin
   - POST /wallet/virtual-account/process
   - GET /misc/datafields/{categoryName}
   - PATCH /user/update

2. KYC Controller (lib/app/controllers/kyc_controller.dart)
   - Tracks: emailVerified, phoneVerified, ninSet, bioDataCompleted, walletCreated
   - Calculates completion percentage
   - Fetches account status on init

3. Updated KYC Completion Banner (lib/app/widgets/kyc_completion_banner.dart)
   - Shows 4 steps with progress bar
   - Each step is clickable and shows completion status
   - Steps: Bio Data, Email, NIN, Wallet PIN

🔨 TODO - NEXT STEPS:

4. Add Routes to app_routes.dart:
   static const COMPLETE_BIODATA = '/complete-biodata';
   static const EMAIL_VERIFICATION = '/email-verification';
   static const WALLET_PIN_SETUP = '/wallet-pin-setup';

5. Create Screen: Complete Bio Data
   - lib/app/screens/kyc/complete_biodata_screen.dart
   - Fetch fields from /misc/datafields/biodata
   - Allow skip option
   - Submit to PATCH /user/update

6. Create Screen: Email Verification
   - lib/app/screens/kyc/email_verification_screen.dart
   - Input email
   - Send OTP via POST /auth/send-otp/email
   - Verify with POST /auth/verify-email-otp
   - 4-digit OTP input

7. Create Screen: NIN Verification (Already exists, needs update)
   - Update lib/app/screens/kyc/nin_verification_screen.dart
   - Input 11-digit NIN
   - POST /misc/verify-nin
   - Show NIN details confirmation
   - OTP input screen
   - POST /misc/confirm-nin

8. Create Screen: Wallet PIN Setup
   - lib/app/screens/kyc/wallet_pin_setup_screen.dart
   - 4-digit PIN input
   - Confirm PIN
   - POST /wallet/virtual-account/process
   - Show wallet details (account number, bank name)

9. Update app_pages.dart:
   - Add GetPage entries for all 4 screens

10. Update user_home_screen.dart:
    - Initialize KYCController on login
    - Call fetchAccountStatus()

11. Test Flow:
    - Login → Check account status
    - Show KYC banner if incomplete
    - Complete each step
    - Verify API calls
    - Check completion status updates
